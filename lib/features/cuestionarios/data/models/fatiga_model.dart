// Archivo: fatiga_model.dart

class PreguntaConfig {
  final String texto;
  // Indica cuál es la respuesta "Segura" (true = SÍ, false = NO)
  final bool respuestaIdeal;

  PreguntaConfig(this.texto, this.respuestaIdeal);
}
